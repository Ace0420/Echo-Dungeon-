// Dungeon core - game logic (voice agnostic)
function generateMap() {
    // Your existing map generation
}

function movePlayer(direction) {
    // Your existing movement logic
}

function combat() {
    // Your existing combat logic
}

function exploreRoom() {
    // Your existing exploration logic
}

function useItem(itemName) {
    // Your existing item usage
}

// Export functions
window.generateMap = generateMap;
window.movePlayer = movePlayer;
window.combat = combat;
window.exploreRoom = exploreRoom;
window.useItem = useItem;
